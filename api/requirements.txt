# Vercel serverless runtime requirements — lightweight API layer only.
# Heavy packages (crewai, reportlab, Pillow, anthropic) are lazy-imported
# inside agent functions and must be installed locally to run the pipeline.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
supabase>=2.3.0
python-dotenv>=1.0.0
pydantic>=2.7.0
httpx>=0.27.0
